<UserControl x:Class="HCI_2024.Views.Dialogs.ConfirmationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HCI_2024.Views.Dialogs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource PrimaryBrush}">
    <StackPanel Margin="20">
        <TextBlock Text="{Binding Message}"
                   FontSize="16"
                   Margin="0 0 0 20"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource TextBrush}"/>
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Button Content="{Binding Source={StaticResource Loc}, Path=[Yes]}"
                    Click="Yes_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="0 0 10 0"
                    Background="{DynamicResource SecondaryBrush}"
                    Foreground="{DynamicResource PrimaryBrush}"/>
            <Button Content="{Binding Source={StaticResource Loc}, Path=[No]}"
                    Click="No_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="10 0 0 0"
                    Background="{DynamicResource SecondaryBrush}"
                    Foreground="{DynamicResource PrimaryBrush}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
