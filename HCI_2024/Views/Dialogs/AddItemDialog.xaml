<UserControl x:Class="HCI_2024.Views.Dialogs.AddItemDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HCI_2024.Views.Dialogs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource PrimaryBrush}">
    <StackPanel Margin="20">
        <!--Message-->
        <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[AddItem]}"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,20"
                   Foreground="{DynamicResource TextBrush}"/>
        <!--Name-->
        <TextBox Width="200"
                 x:Name="NameInput"
                 HorizontalAlignment="Center"
                 Margin="0 0 0 10"
                 Text="{Binding Name, Mode=TwoWay}"
                 materialDesign:HintAssist.Hint="{Binding Source={StaticResource Loc}, Path=[ItemName]}"
                 Foreground="{DynamicResource TextBrush}"
                 Background="{DynamicResource BackgroundBrush}"
                 BorderBrush="{DynamicResource BorderBrush}"/>
        <!--Picture Url-->
        <TextBox Width="200"
                 x:Name="PictureUrlInput"
                 HorizontalAlignment="Center"
                 Margin="0 0 0 10"
                 Text="{Binding PictureUrl, Mode=TwoWay}"
                 materialDesign:HintAssist.Hint="{Binding Source={StaticResource Loc}, Path=[PictureUrl]}"
                 Foreground="{DynamicResource TextBrush}"
                 Background="{DynamicResource BackgroundBrush}"
                 BorderBrush="{DynamicResource BorderBrush}"/>
        <!--Price-->
        <TextBox Width="200"
                 x:Name="PriceInput"
                 HorizontalAlignment="Center"
                 Margin="0 0 0 20"
                 Text="{Binding Price, Mode=TwoWay}"
                 materialDesign:HintAssist.Hint="{Binding Source={StaticResource Loc}, Path=[Price]}"
                 Foreground="{DynamicResource TextBrush}"
                 Background="{DynamicResource BackgroundBrush}"
                 BorderBrush="{DynamicResource BorderBrush}"/>
        <!--Type-->
        <ComboBox Width="200"
                  x:Name="TypeInput"
                  HorizontalAlignment="Center"
                  Margin="0 0 0 20"
                  ItemsSource="{Binding ItemTypes}"
                  SelectedItem="{Binding SelectedItemType, Mode=TwoWay}"
                  Foreground="{DynamicResource PrimaryBrush}"
                  Background="{DynamicResource BackgroundBrush}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  materialDesign:HintAssist.Hint="{Binding Source={StaticResource Loc}, Path=[Type]}"/>
        <!--Buttons-->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="{Binding Source={StaticResource Loc}, Path=[Confirm]}"
                Command="{Binding AddItemCommand}"
                Height="40"
                Padding="10"
                Margin="0 0 10 0"
                Background="{DynamicResource SecondaryBrush}"
                Foreground="{DynamicResource PrimaryBrush}"
                FontWeight="Bold"
                HorizontalAlignment="Center"/>
            <Button Content="{Binding Source={StaticResource Loc}, Path=[Cancel]}"
                Command="{Binding CancelCommand}"
                Height="40"
                Padding="10"
                Margin="10 0 0 0"
                Background="{DynamicResource SecondaryBrush}"
                Foreground="{DynamicResource PrimaryBrush}"
                FontWeight="Bold"
                HorizontalAlignment="Center"/>
        </StackPanel>
        <!--Snackbar-->
        <materialDesign:Snackbar Margin="0,20,0,0"
                                 MessageQueue="{Binding SnackbarMessageQueue}"
                                 HorizontalAlignment="Center"
                                 Background="{DynamicResource SecondaryBrush}"
                                 Foreground="{DynamicResource PrimaryBrush}"/>
    </StackPanel>
</UserControl>
